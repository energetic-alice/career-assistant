node_modules/
dist/
test-output/
# Runtime data only (pipelineStates, market-index, logs). Source code lives
# in src/data/ and must NOT be ignored (see exceptions below).
/data/
*.json
!package.json
!package-lock.json
!tsconfig.json
!src/data/**
.env
